# OpenAI API key (replace with your key locally)
OPENAI_API_KEY=your_api_key_here

# Database URL (SQLite local dev)
DATABASE_URL="file:./dev.db"

# Optional: environment and port
NODE_ENV=development
PORT=3001
